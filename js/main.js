let isJamAllowed = false;

const events = [
  {
    description: "on click event handler for attaching comment box.",
    type: "",
    handler: "",
  },
];

function createCommentBox() {
  const containerDiv = document.createElement("div");
}
